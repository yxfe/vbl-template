import 'vbl.css';
import vblModal from 'vbl-modal';
import vblAlert from 'vbl-alert';
import vblDatetime from 'vbl-datetime';
import vblDropdown from 'vbl-dropdown';
import vblPage from 'vbl-page';
import vblTree from 'vbl-tree';
import vblTable from 'vbl-table';
import vblInput from 'vbl-input';
import vblTextarea from 'vbl-textarea';
import vblIcon from 'vbl-icon';
import vblButton from 'vbl-button';
import vblLoading from 'vbl-loading';
import vblPopover from 'vbl-popover';
import vblCheckbox from 'vbl-checkbox';
import vblRadio from 'vbl-radio';
import vblSelect from 'vbl-select';
import vblForm from 'vbl-form';
import vblAutocomplete from 'vbl-autocomplete';
import vblTab from 'vbl-tab';
import vblLayout from 'vbl-layout';
import vblBreadcrumb from 'vbl-breadcrumb';
import vblUpload from 'vbl-upload';
import vblAuth from 'vbl-Auth';

export default {
    vblModal,
    vblAlert,
    vblDatetime,
    vblDropdown,
    vblPage,
    vblTree,
    vblTable,
    vblInput,
    vblTextarea,
    vblIcon,
    vblButton,
    vblLoading,
    vblPopover,
    vblCheckbox,
    vblRadio,
    vblSelect,
    vblForm,
    vblAutocomplete,
    vblTab,
    vblLayout,
    vblBreadcrumb,
    vblUpload,
    vblAuth
};